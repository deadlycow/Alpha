@model MemberLoginForm

<form method="post" novalidate asp-controller="Auth" asp-action="Login">
    <div class="form-input">
        <label asp-for="Email"></label>
        <input id="email" asp-for="Email" class="input-field" type="email" />
        <span asp-validation-for="Email"></span>
    </div>
    <div class="form-input password-container">
        <label asp-for="Password"></label>
        <input id="password" asp-for="Password" class="input-field" type="password" />
        <img class="toggle-password eye-icon" src="/images/eye-closed.svg" alt="Toggle Password">
        <span asp-validation-for="Password"></span>
    </div>
    <div class="form-me">
        <div>
            <input id="rememberMe" type="checkbox" />
            <label for="rememberMe">Remember me</label>
        </div>
        <a href="#">Forgot Password?</a>
    </div>
    <button class="btn btn-primary" type="submit">Login</button>
</form>

@if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
{
    <p>@ViewBag.ErrorMessage</p>
}